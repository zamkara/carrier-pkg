name: release

on:
  push:
    branches: [master]
    tags: [v*]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable

      - name: set version
        id: version
        run: |
          if [[ "${{ github.ref }}" == refs/tags/v* ]]; then
            VER="${GITHUB_REF_NAME#v}"
            TAG="$GITHUB_REF_NAME"
          else
            VER="0.1.0-${GITHUB_SHA:0:8}"
            TAG="ci-${GITHUB_SHA:0:8}"
          fi
          echo "version=$VER" >> "$GITHUB_OUTPUT"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"

      - name: inject version
        run: |
          sed -i "s/^version = \".*\"/version = \"${{ steps.version.outputs.version }}\"/" Cargo.toml

      - run: cargo build --release

      - name: package
        run: |
          ARCHIVE="carrier-${{ steps.version.outputs.version }}-x86_64-linux.tar.gz"
          mkdir -p dist
          tar czf "dist/$ARCHIVE" -C target/release carrier
          echo "archive=$ARCHIVE" >> "$GITHUB_ENV"

      - name: create release
        env:
          TAG: ${{ steps.version.outputs.tag }}
          VERSION: ${{ steps.version.outputs.version }}
          GH_TOKEN: ${{ github.token }}
        run: |
          PRERELEASE=""
          if [[ "${{ github.ref }}" != refs/tags/v* ]]; then
            PRERELEASE="--prerelease"
          fi
          if gh release view "$TAG" &>/dev/null; then
            echo "release $TAG already exists — uploading assets"
          else
            gh release create "$TAG" \
              --title "carrier $VERSION" \
              --notes "" \
              --generate-notes \
              $PRERELEASE
          fi
          gh release upload "$TAG" "dist/${{ env.archive }}" --clobber
